{
  "name": "tinynpm",
  "displayName": "tinyNpm",
  "description": "Security focused npm package.json companion",
  "version": "0.0.1",
  "publisher": "about14sheep",
  "license": "MIT",
  "icon": "images/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/about14sheep/tinyNpm.git"
  },
  "bugs": {
    "url": "https://github.com/about14sheep/tinyNpm/issues"
  },
  "homepage": "https://github.com/about14sheep/tinyNpm#readme",
  "keywords": [
    "npm",
    "package.json",
    "security",
    "dependencies",
    "supply chain"
  ],
  "engines": {
    "vscode": "^1.107.0"
  },
  "categories": [
    "Linters",
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "configuration": {
      "title": "tinynpm user configuration",
      "properties": {
        "tinynpm.versionBufferPeriod": {
          "type": "number",
          "default": 3,
          "description": "Minimum age in days for package versions (helps avoid newly published malicious packages)"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/semver": "^7.7.1",
    "@types/vscode": "^1.107.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.27.1",
    "eslint": "^9.39.1",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.48.1"
  },
  "dependencies": {
    "semver": "^7.7.3"
  }
}
